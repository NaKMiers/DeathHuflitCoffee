@* @model List<DeathWishCoffee.Models.Main.Order> *@
@{
    ViewData["Title"] = "All Orders Statistic";
    var userId = ViewBag.UserId;
}

<div class="bg-light pt-5 overflow-scroll">
    <a href="/admin" class="btn btn-secondary mb-3">
        Back
    </a>

    <h2 class="AllOrdersByUser__tiltle">Orders Statistic: @userId</h2>
    <table class="table">
        <thead>
            <tr class="AllOderStatistic__Content-type">
                <th class="AllOderStatistic__type">Products</th>
                <th class="AllOderStatistic__type">ID</th>
                <th class="AllOderStatistic__type">Quantity Purchased</th>
                <th class="AllOderStatistic__type">remain</th>
                <th class="AllOderStatistic__type">Status</th>
                <th class="AllOderStatistic__type">CreatedAt</th>
                <th class="AllOderStatistic__type">LastModifiedAt</th>
                <th class="AllOderStatistic__type">Delete</th>
                <th class="AllOderStatistic__type">Edit</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var order in Model)
            {
                <tr>
                    <td class="AllOrderByUser__type">
                        @if (order.OrderDetails != null && order.OrderDetails.Count > 0)
                        {
                            @foreach (var oD in order.OrderDetails)
                            {
                                <p>@oD.Product?.Title</p>
                            }
                        }
                    </td>
                    <td class="AllOrderByUser__type">@order.Id</td>
                    <td class="AllOrderByUser__type">
                    </td>

                    <td class="AllOrderByUser__type">
                        @if (order.OrderDetails != null && order.OrderDetails.Count > 0)
                        {
                            @foreach (var oD in order.OrderDetails)
                            {
                                <p>@oD.Product?.Remain</p>
                            }
                        }
                    </td>
                    <td class="AllOrderByUser__type">@order.Status</td>

                    <td class="AllOrderByUser__type">@order.CreatedAt</td>
                    <td class="AllOrderByUser__type">@order.LastModifiedAt</td>
                    <td class="AllOrderByUser__type">
                        <a href="/admin/orders/delete/@order.Id" class="btn btn-danger">Delete</a>
                    </td>
                    <td class="AllOrderByUser__type">
                        <a href="/admin/orders/edit/@order.Id" class="btn btn-info">Edit</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>